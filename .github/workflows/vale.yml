name: Vale Lint

on:
  pull_request:
    paths:
      - "**/*.md"
  push:
    paths:
      - "**/*.md"

jobs:
  lint:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4

      - name: Install Vale
        uses: errata-ai/vale-action@reviewdog
        with:
          version: latest

      - name: Run Vale
        uses: errata-ai/vale-action@reviewdog
        with:
          files: .
          reporter: github-pr-review
          fail_on_error: false # only warnings
